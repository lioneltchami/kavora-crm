name: Publish pg-test image

on:
  push:
    branches: [main]
    paths:
      - Dockerfile.pg-test
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.pg-test
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/kavora-pg-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
